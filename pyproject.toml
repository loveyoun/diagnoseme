[project]
name = "diagnoseme"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aerich>=0.8.1",
    "asyncpg>=0.31.0",
    "celery[redis]>=5.3.1",
    "fastapi[standard]>=0.135.2",
    "flower>=2.0.1",
    "gunicorn>=25.3.0",
    "httpx>=0.28.1",
    "passlib[bcrypt]>=1.7.4",
    "pwdlib[argon2]>=0.3.0",
    "pyjwt>=2.12.1",
    "python-dotenv>=1.2.2",
    "python-jose[cryptography]>=3.5.0",
    "redis>=7.4.0",
    "tomli-w>=1.2.0",
    "tortoise-orm>=1.1.7",
]

[dependency-groups]
dev = [
    "aiosqlite>=0.22.1",
    "black>=26.3.1",
    "coverage>=7.13.5",
    "locust>=2.43.4",
    "mypy>=1.19.1",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "ruff>=0.15.7",
]

[tool.aerich]
tortoise_orm = "app.db.database.TORTOISE_ORM"
location = "./migrations"
src_folder = "./."

[tool.black]
line-length = 120

[tool.mypy]
plugins = [
    "pydantic.mypy",
]
python_version = 3.14
strict = true

[tool.pytest.ini_options]
asyncio_mode = "auto"  # @pytest.mark.asyncio
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
# asyncio_default_fixture_loop_scope = "session"
